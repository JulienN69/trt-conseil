{% extends 'base.html.twig' %}

{% block title %}Hello AnnouncementController!{% endblock %}

{% block body %}



<h2>Créer une annonce d'emploi</h2>

{% include 'partials/flash.html.twig' %}

{{ form_start(AnnouncementForm) }}
{{ form_row(AnnouncementForm.jobTitle) }}
{{ form_row(AnnouncementForm.workPlace) }}
{{ form_row(AnnouncementForm.description) }}
{{ form_row(AnnouncementForm.recruiter, {
    'row_attr': {
        'class':'d-none'
    }
}) }}



{# {% if is_granted('ROLE_USER') and app.user.recruiter is not null %}

<input type="hidden" value={ app.user.recruiter.id } name="recruiter">
{% endif %} #}

{# {% if is_granted('ROLE_USER') and app.user.recruiter is not null %} #}
    {# <input type="hidden" value="{{ app.user.recruiter.id }}" name="job_offer[recruiter]"> #}
    {# {{ form_row(jobOfferForm.recruiter, {'value': app.user}) }}
{% endif %} #}


<button type="submit" class="btn btn-primary">Valider</button>
{{ form_end(AnnouncementForm) }}

{% endblock %}

